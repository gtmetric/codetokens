Add a new `'bulk'` tier that applies a 33% discount (i.e. the customer pays 67%
of the price). Extend the `Tier` type to include `'bulk'` and handle it in
`discountedCents`. The existing `'member'` tier is unchanged.
